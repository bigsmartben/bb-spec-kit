# EXT-002 测试用例

## 测试策略

验证实验性模板的**可用性**（使用者能正确填写、无歧义）和**完整性**（下游开发/测试能从中获取明确规格）。

---

### TC-001：模板结构完整性

**目标**：确认 4 个新增小节均存在于实验性模板中，且原有小节未被删改

| # | 检查项 | 预期结果 | 实际结果 | 状态 |
|---|--------|---------|---------|------|
| 1 | `Actors & System Boundary` 小节存在 | ✅ | 存在 | 通过 |
| 2 | `User Interaction Flows` 小节存在 | ✅ | 存在 | 通过 |
| 3 | `Entity State Machines` 小节存在 | ✅ | 存在 | 通过 |
| 4 | `UI Component Specification` 小节存在 | ✅ | 存在 | 通过 |
| 5 | `User Scenarios & Testing` 原有内容未变 | ✅ | 保留 | 通过 |
| 6 | `Requirements` 原有内容未变 | ✅ | 保留 | 通过 |
| 7 | `Success Criteria` 原有内容未变 | ✅ | 保留 | 通过 |

---

### TC-002：可选标注正确性

**目标**：确认各小节的 optional/required 标注符合设计决策

| 小节 | 预期标注 | 实际标注 | 状态 |
|------|---------|---------|------|
| Actors & System Boundary | `*(optional)*` | `*(mandatory)*`（在 UC 化重构后由全局上下文替代） | 通过（结构变更已记录） |
| User Interaction Flows | `*(optional)*` | `*(optional)*` | 通过 |
| Entity State Machines | `*(optional)*` | `*(optional)*` | 通过 |
| UI Component Specification | `*(required for frontend features)*` | `*(required for frontend features)*` | 通过 |

---

### TC-003：Edge Cases 注释正确性

**目标**：确认 Edge Cases 小节注释中明确说明了迁移规则

| # | 检查项 | 预期结果 | 状态 |
|---|--------|---------|------|
| 1 | 注释说明"状态转换类 → 移至 §2.2 全局状态机总览" | ✅ | 通过 |
| 2 | 注释说明"环境类异常保留此处" | ✅ | 通过 |

---

### TC-004：ID 引用关系完整性

**目标**：确认各层之间的 ID 引用规则在模板注释中有说明

| 引用关系 | 预期说明位置 | 状态 |
|---------|------------|------|
| UC ID → User Story | §2 UC 总览与明细链接 | 通过 |
| Flow 节点 → FR-xxx | §3.2 注释 | 通过 |
| Flow 异常路径 → EC-xxx | §3.2 注释与异常路径表 | 通过 |
| UI 口径 → Key Entities ref | §3.4 口径字段注释 | 通过 |
| State Machine → Key Entities | §2.2 小节标题 ref 格式 | 通过 |

---

### TC-005：内涵与口径必填性

**目标**：确认 UI Component Specification 中内涵和口径字段有"必填"强制说明

| # | 检查项 | 预期结果 | 状态 |
|---|--------|---------|------|
| 1 | "内涵"字段标注"**必填**" | ✅ | 通过 |
| 2 | "口径"字段标注"**必填**" | ✅ | 通过 |
| 3 | 内涵字段提供正例（好）和反例（差） | ✅ | 通过 |
| 4 | 口径字段提供 ref 引用格式示例 | ✅ | 通过 |

---

### TC-006：Sandbox 实际填写验证 *(Step 3 执行)*

**目标**：使用真实 PRD 完整填写模板，验证引导是否足够清晰

**测试 PRD**：`.spec-workspace/sandbox/test-prds/` 下选取（或创建）

| 检查项 | 预期结果 | 实际结果 | 状态 |
|--------|---------|---------|------|
| 能从 PRD 中直接提取 Actor 清单，无需补充调研 | 无信息缺口 | — | 待测 |
| 每条 User Story 能对应至少一条交互流程 | 全覆盖 | — | 待测 |
| 状态枚举能穷举（填写时无"漏了一个状态"的困惑） | 无遗漏 | — | 待测 |
| UI 组件内涵与口径均能填写（不会"不知道填什么"） | 无空项 | — | 待测 |
| 产品、开发、测试三方阅读后歧义点为 0 | 0 歧义 | — | 待测 |

---

### TC-007：向后兼容性

**目标**：确认现有 Spec 文件不受影响

| # | 检查项 | 预期结果 | 状态 |
|---|--------|---------|------|
| 1 | 新增小节缺失不影响模板的整体可读性 | ✅ | 通过（模板注释与可选标识明确） |
| 2 | 未填写可选小节的 Spec 仍符合 Spec Kit 规范 | ✅ | 通过（保留 mandatory 核心骨架） |
| 3 | 主仓库同步后，现有 `/speckit.specify` 生成的 spec.md 格式不变 | ✅ | 待主仓库同步后验证 |

---

### TC-008：闭环模板链路一致性（新增）

**目标**：验证“Spec 业务锚点 -> Plan 实现映射 -> Tasks 双来源拆解”闭环规则已落地。

| # | 检查项 | 预期结果 | 实际结果 | 状态 |
|---|--------|---------|---------|------|
| 1 | `plan-template-EXT-002.md` 存在 `Upstream Anchors` | ✅ | 存在 | 通过 |
| 2 | `plan-template-EXT-002.md` 存在 `Interface & Implementation Mapping` | ✅ | 存在 | 通过 |
| 3 | `tasks-template-EXT-002.md` 明确双来源规则 | ✅ | 已声明 | 通过 |
| 4 | `commands/plan-EXT-002.md` 含冲突阻断条件 | ✅ | 已声明 | 通过 |
| 5 | `commands/tasks-EXT-002.md` 要求追溯到 `Entity.field` | ✅ | 已声明 | 通过 |

---

### TC-009：Spec 去实现化约束（新增）

**目标**：验证 Spec 示例文档不包含实现细节（接口/HTTP/存储映射等）。

| # | 检查项 | 预期结果 | 实际结果 | 状态 |
|---|--------|---------|---------|------|
| 1 | 示例 Spec 不出现 endpoint（如 `GET /...`、`POST /...`） | ✅ | 已清理 | 通过 |
| 2 | 示例 Spec 不出现 HTTP 状态码分支（如 200/4xx/5xx） | ✅ | 已清理 | 通过 |
| 3 | 示例 Spec 不出现 DB 实现术语（事务、表结构、row-level/version） | ✅ | 已清理 | 通过 |
| 4 | 业务一致性语义仍可验收（原子性/并发恢复/错误反馈） | ✅ | 保留 | 通过 |

---

## 测试执行记录

| 测试轮次 | 日期 | 执行人 | 通过数 | 失败数 | 备注 |
|---------|------|-------|--------|-------|------|
| Round-1 | 2026-03-03 | Copilot | 30 | 0 | 完成模板结构、注释规则、闭环链路与“去实现化”静态验证；保留主仓库同步后兼容性回归 |
