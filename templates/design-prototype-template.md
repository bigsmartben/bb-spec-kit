---
description: "Static HTML prototype template pack for speckit.design outputs (hub, pages, shared assets, and local viewing instructions)."
---

# Design Prototype Templates: [FEATURE NAME]

**Feature Branch**: `[###-feature-name]`  
**Created**: [DATE]  
**Input**: `spec.md` (required), `plan.md` (optional constraints)

Use this template as a structure baseline for the static prototype generated by `/speckit.design` under `prototype/`.

---

## Prototype Files *(mandatory)*

### `prototype/index.html`

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>[Feature] Prototype Hub</title>
    <link rel="stylesheet" href="./assets/styles.css" />
  </head>
  <body>
    <header>
      <h1>[Feature] Prototype</h1>
      <nav>
        <!-- Link to ./pages/*.html -->
      </nav>
    </header>
    <main>
      <p>[Brief description and usage hints]</p>
    </main>
    <script src="./assets/app.js"></script>
  </body>
</html>
```

### `prototype/pages/[screen].html`

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>[Screen Name]</title>
    <link rel="stylesheet" href="../assets/styles.css" />
  </head>
  <body>
    <main>
      <h1>[Screen Name]</h1>
      <!-- Include representative normal/loading/empty/error states -->
    </main>
    <script src="../assets/app.js"></script>
  </body>
</html>
```

### `prototype/assets/styles.css`

```css
:root {
  --color-bg: #ffffff;
  --color-text: #111111;
  --color-primary: #0b57d0;
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --radius-1: 6px;
}

/* Add responsive layout + visible focus + reduced-motion support */
```

### `prototype/assets/app.js`

```javascript
// Mock data and tiny interaction helpers for tabs/modals/toasts/form validation.
// Keep dependency-free and easy to read.
```

### `prototype/README.md`

```markdown
# Prototype Readme

## View Locally

- Open `index.html` directly in a browser, or
- Run a static server:
  - `python -m http.server 8000`
  - `npx serve .`
```
